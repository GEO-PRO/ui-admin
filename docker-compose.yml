version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run start
    restart: unless-stopped
    ports:
      - "3002:3002"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
